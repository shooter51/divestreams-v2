# Test Environment Configuration
# Used for integration tests with local Docker database

# Database
DATABASE_URL=postgresql://test_user:test_password@localhost:5433/divestreams_test

# Redis
REDIS_URL=redis://localhost:6380

# Application
APP_URL=http://localhost:5173
AUTH_SECRET=test-auth-secret-key-for-testing-only
SESSION_SECRET=test-session-secret-key-for-testing-only

# Admin
ADMIN_PASSWORD=TestAdmin123
PLATFORM_ADMIN_EMAIL=admin@test.local
PLATFORM_ADMIN_PASSWORD=PlatformAdmin123

# Email (use MailHog or fake SMTP for tests)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@test.local

# Stripe (test keys)
STRIPE_SECRET_KEY=sk_test_mock_key_for_testing
STRIPE_PUBLISHABLE_KEY=pk_test_mock_key_for_testing
STRIPE_WEBHOOK_SECRET=whsec_test_mock_secret_for_testing

# Storage (mock for tests)
B2_ENDPOINT=
B2_REGION=
B2_BUCKET=
B2_KEY_ID=
B2_APP_KEY=
CDN_URL=

# Feature flags for testing
NODE_ENV=test
