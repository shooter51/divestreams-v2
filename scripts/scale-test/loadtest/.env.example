# Platform admin credentials for tenant discovery
ADMIN_EMAIL=admin@divestreams.com
ADMIN_PASSWORD=PlatformAdmin2026!

# Worker configuration
WORKER_COUNT=10
METRICS_INTERVAL_MS=30000
SESSION_TIMEOUT_MS=15000

# Target domain
BASE_DOMAIN=test.divestreams.com

# Enable registration flow (creates test accounts)
LOAD_TEST_REGISTER=false

# Self-test mode: 1 worker, 3 tenants, 2 min max
SELF_TEST=false
